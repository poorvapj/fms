# Where the backend API runs (dev/preview proxy target)
# VITE_API_PROXY=http://localhost:4600
# Only when the frontend is hosted on a different domain from the API:
# VITE_API_URL=https://fms-api.example.com
