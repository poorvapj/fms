# FMS Operations frontend — Render Static Site build (npm run build:render).
# Address of the backend Web Service. Replace with your real backend URL (no trailing slash).
VITE_API_URL=https://fms-backend.onrender.com
